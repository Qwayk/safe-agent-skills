# Jobs and batches

This tool intentionally does not implement batch jobs. It is read-only and only supports interactive query commands.

If you need batch behavior, add it only after a safety review, and update `docs/safety_model.md`, tests, and `AGENTS.md` together.
