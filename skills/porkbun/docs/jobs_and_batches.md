# Jobs and batches

This tool does not include a jobs/batches command path.

Run one explicit command at a time with reviewed intent.

If you need multiple tasks, send them as separate user steps or automate outside this tool where command-level approval is still explicit.
