{
  "ok": true,
  "dry_run": false,
  "refused": true,
  "reasons": [
    "Refused: Figma write apply requires explicit no-snapshot approval until this command saves real before-state before provider writes"
  ],
  "refusal_type": "SafetyError",
  "operation": {
    "area": "comments",
    "op_key": "post-comment",
    "title": "Add comment",
    "method": "POST",
    "path_template": "/v1/files/{file_key}/comments",
    "safety": {
      "mode": "write",
      "requires_yes": true,
      "requires_ack_irreversible": false,
      "live_verification_note": "Pending live verification once this operation is wired into the runtime and auth mode is connected"
    }
  },
  "request": {
    "method": "POST",
    "path": "/v1/files/file_123/comments",
    "url": "http://127.0.0.1:8765/v1/files/file_123/comments",
    "query": {},
    "body": {
      "comment": "Hello from the local proof stub"
    },
    "headers": {},
    "verification_note": "Write apply requires explicit no-snapshot approval before provider HTTP until operation-specific saved snapshot support is available."
  },
  "plan": {
    "operation_id": "comments:post-comment",
    "method": "POST",
    "path": "/v1/files/file_123/comments",
    "url": "http://127.0.0.1:8765/v1/files/file_123/comments",
    "query": {},
    "body": {
      "comment": "Hello from the local proof stub"
    },
    "env_fingerprint": "http://127.0.0.1:8765|personal",
    "verification_note": "Write apply requires explicit no-snapshot approval before provider HTTP until operation-specific saved snapshot support is available.",
    "before_state": {
      "required": true,
      "supported": false,
      "status": "blocked",
      "operation": "comments:post-comment",
      "target": {
        "method": "POST",
        "path": "/v1/files/file_123/comments",
        "path_params": {
          "file_key": "file_123"
        },
        "query": {}
      },
      "saved_path": null,
      "provider_backup_id": null,
      "reason": "This Figma write family does not yet capture the live resource state or provider recovery point before apply."
    },
    "verification_plan": {
      "type": "no-snapshot-approval",
      "status": "blocked-before-provider-write",
      "notes": "Apply must require explicit no-snapshot approval before Figma token use or HTTP until operation-specific saved snapshot support is available."
    },
    "post_apply_verification_note": "Write apply requires explicit no-snapshot approval before provider HTTP until operation-specific saved snapshot support is available.",
    "recovery": {
      "automatic_rollback": false,
      "end_state": "irreversible_and_clearly_labeled",
      "strategy": "no_inverse",
      "rollback_ready": false,
      "backups": [],
      "snapshots": [],
      "rollback_plan": null,
      "restore_note": "comments:post-comment: no automated rollback is available for this command; it is treated as irreversible and needs manual cleanup if removal is required."
    }
  },
  "verification_note": "Write apply requires explicit no-snapshot approval before provider HTTP until operation-specific saved snapshot support is available.",
  "verification_plan": {
    "type": "no-snapshot-approval",
    "status": "blocked-before-provider-write",
    "notes": "Apply must require explicit no-snapshot approval before Figma token use or HTTP until operation-specific saved snapshot support is available."
  }
}
