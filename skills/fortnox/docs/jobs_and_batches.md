# Jobs and batches

`jobs run` is reserved for future registry-backed Fortnox batch rows.
The old template ping rows were removed, and no real public batch registry ships in this tool yet.

For now, use explicit Fortnox commands one resource at a time so the safety plan, review, and receipt stay tied to real documented operations.
