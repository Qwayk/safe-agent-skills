"""Legacy scaffold note for older imports.

The shipped CallRail CLI uses `CALLRAIL_API_TOKEN` from `.env` and does not
expose token-json storage commands. This module remains only as a compatibility
stub because the repo rules for this autonomous run do not allow silent file
deletion.
"""
