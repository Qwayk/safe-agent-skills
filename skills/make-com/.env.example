# Make.com API configuration. Copy to .env and keep .env local-only.
# Choose your Make zone. The CLI appends /api/v2 when it is omitted.
MAKE_BASE_URL=https://eu1.make.com
MAKE_API_TOKEN=
MAKE_TIMEOUT_S=30
