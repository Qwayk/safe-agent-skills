# Example env file for *offline* plan generation only.
#
# This file is safe to commit because the API key is a dummy placeholder.
# Never commit real Stripe keys.

STRIPE_API_KEY=sk_test_dummy_DO_NOT_USE
STRIPE_TIMEOUT_S=30
STRIPE_VERSION=
STRIPE_ACCOUNT_ALLOWLIST=acct_allowed

