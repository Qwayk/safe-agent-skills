# Configuration

Required local settings:

- `N8N_BASE_URL`: n8n API root ending in `/api/v1`
- `N8N_API_KEY`: n8n API key

Optional:

- `N8N_TIMEOUT_S`: request timeout in seconds, default `30`

Keep `.env` local-only. It is ignored by Git.
