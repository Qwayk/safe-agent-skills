# Examples (redacted)

All files under this folder are committed, redacted examples:
- No real domains (use `example.com`).
- No real IDs/usernames/emails.
- No secrets or auth headers.

Suggested structure:
- `outputs/*.json`: CLI stdout examples (one JSON object each).
- `plan.example.json`: representative dry-run plan output (v2).
- `receipt.example.json`: representative apply receipt output (v2).

