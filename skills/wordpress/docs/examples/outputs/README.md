# Output examples

Each JSON file in this folder should be the **entire stdout** of one CLI invocation in `--output json` mode.

Redaction rules:
- Use `example.com` / `https://example.com`.
- Use fake IDs.
- Do not include secrets or auth headers.

