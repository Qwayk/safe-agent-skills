# Concepts

- **Dry-run**: default mode; shows what would change.
- **Apply**: add `--apply` to write changes.
- **Verify**: after applying, the tool re-fetches and asserts the new values match.
- **Caption policy**: “media caption” is different from “post caption”.

