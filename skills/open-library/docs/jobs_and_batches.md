# Jobs and batches

This command family is not included in this tool.

This release is read-only only and exposes no batch job or write actions.
Use `docs/command_reference.md` for the supported command list.
