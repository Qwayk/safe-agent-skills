[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qwayk-xero-safe-agent-cli"
version = "0.1.0"
description = "Xero API CLI with fixed commands and review-first writes for agent use."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "PyYAML>=6.0.2",
  "requests>=2.31.0",
]

[project.optional-dependencies]
dev = [
  "build>=1.2.0",
  "mypy>=1.8.0",
  "ruff>=0.6.0",
  "types-requests>=2.31.0.0",
  "types-PyYAML>=6.0.12.20250516",
]

[tool.setuptools.package-data]
xero_safe_agent_cli = ["generated/*.json"]

[project.scripts]
qwayk-xero-safe-agent-cli = "xero_safe_agent_cli.__main__:main"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = false

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["xero_safe_agent_cli.commands*"]

[tool.ruff]
target-version = "py312"
line-length = 100
exclude = [".venv", "build", "dist", "src/**.egg-info"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.12"
mypy_path = "src"
warn_unused_ignores = true
warn_redundant_casts = true
no_implicit_optional = true
check_untyped_defs = true
show_error_codes = true
pretty = false
exclude = "(^\\.venv/|^build/|^dist/|^src/.*\\.egg-info/)"
