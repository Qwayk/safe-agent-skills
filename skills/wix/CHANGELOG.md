# Changelog

All notable changes to this project are documented in this file.

This format is based on Keep a Changelog, and this project follows Semantic Versioning.
Because this tool is pre-1.0 (`0.x`), minor version bumps may include breaking changes.

## [Unreleased]

### Added

- Added the official Wix Viewer Cache and SEO Tags slice with explicit `viewer-cache invalidate`, `viewer-seo-tags resolve-item`, and `viewer-seo-tags resolve-static` commands, cache invalidation plan safety, SEO read helpers, official inventory, command docs, proof, and focused tests.
- Added the official Developer Preview Wix Partner Profile V1 family with explicit `partner-profiles create|update|delete|get-current|get-public|find-public-by-slug` commands, public no-auth reads, account API-key owner commands, irreversible acknowledgement for delete, first-party-only contact boundary, official inventory, command docs, proof, and focused tests.
- Added the official Wix B2B Site Management Business Site Transfer V1 method with explicit `b2b-site-transfer transfer`, account API-key auth, target-account header handling, irreversible transfer acknowledgement, official inventory, command docs, proof, and focused tests.
- Added the official Wix Online Programs Instructor V2 subfamily with explicit `online-programs-instructor-v2 create|update|query|assign|change-program-instructors|invite|list|unassign` commands, reviewed-plan writes, irreversible acknowledgement for invite and assignment removals/changes, official inventory, command docs, proof, and focused tests.
- Split the broad Online Programs row and added the official Wix Online Programs Programs subfamily with explicit `online-programs-programs create|get|update|delete|query|search|count|bulk-update|archive|duplicate|end|list-samples|publish` commands, reviewed-plan writes, revision validation, irreversible acknowledgements for delete/archive/end, official inventory, command docs, proof, and focused tests.
- Added official Wix Multilingual Machine Translation Credit Data coverage with explicit read/helper `multilingual-machine-translation-credit-data get|check-sufficient` commands, official credit-data inventory, command docs, wrapper docs, proof, and focused tests.
- Added official Wix Multilingual Machine Translation coverage with explicit `multilingual-machine-translation translate|bulk-translate` commands, plan-first credit-spend safety, bulk limits, and focused tests.
- Added official Wix Multilingual Translation Published Content coverage with explicit read-only `multilingual-translation-published-contents query`, required schema-key filter validation, and callback-only published-content event inventory.
- Added official Wix Multilingual Translation Content coverage with explicit `multilingual-translation-contents create|get|update|delete|query|search|bulk-create|bulk-delete|bulk-update|bulk-update-by-key|update-by-key` commands, plan-first writes, bulk limits, key-based updates, callback-only event inventory, and irreversible acknowledgement for deletes and field-removal updates.
- Added official Wix Multilingual Translation Schema coverage with explicit `multilingual-translation-schemas create|get|update|delete|query|list-site|get-by-key` commands, plan-first writes, revision validation, and irreversible acknowledgement for schema deletes and field-removal updates.
- Added the current callable Wix Headless Sitemap family as an explicit read-only named command under `headless-sitemap`, covering list-pages with official item-type validation, cursor paging, endpoint-mismatch documentation, and focused local tests.
- Added the current callable Developer Preview Wix Headless Verification family as an explicit named command under `headless-verification`, covering verify-during-authentication with reviewed-plan safety, email-verification code/state-token redaction, returned session-token redaction, endpoint-mismatch documentation, and focused local tests.
- Added the current callable Wix Headless Redirects family as an explicit named command under `headless-redirects`, covering create-redirect-session with reviewed-plan safety, irreversible acknowledgement for single-use visitor-flow URLs, session-token redaction, official published-site and allowed-redirect prerequisites, endpoint-mismatch documentation, and focused local tests.
- Added the current callable Wix Headless Recovery family as an explicit named command under `headless-recovery`, covering send-recovery-email with reviewed-plan safety, irreversible acknowledgement for the password-reset email side effect, official published-site and allowed-redirect prerequisites, and focused local tests.
- Added the current callable Wix Headless Authentication family as explicit named commands under `headless-authentication`, covering login-v2, retrieve-tokens, register-v2, change-password, logout, and sign-on with sensitive token/password/code/cookie redaction, reviewed-plan safety for member/session-changing actions, and irreversible acknowledgement for password change and trusted sign-on.
- Added the current callable Wix Members Activity Counters family as explicit named commands under `activity-counters`, covering get, query, set, Activity Counter Updated event inventory, wrapper coverage, reviewed-plan safety for set, and focused local tests.
- Classified the deprecated Wix Members Activity Badges v3 family as non-callable, with official inventory coverage for the legacy badge methods and deprecated badge events that are replaced by Badges V4 or Badge Assignments.
- Added the current callable Wix Members Activity Badges V4 family as explicit named commands under `badges-v4`, covering get, query, create, update, delete, move, deprecated display-order inventory, badge event inventory, wrapper coverage, irreversible acknowledgement for delete, and focused local tests.
- Added the current callable Wix Members Activity Badge Assignments family as explicit named commands under `badge-assignments`, covering query, create, delete, bulk create, bulk delete, bulk tag updates, bulk tag updates by filter, badge-assignment event inventory, wrapper coverage, irreversible acknowledgement for deletes and broad filter tag updates, and focused local tests.
- Added the current callable Wix Members Activity Member Reports family as explicit named commands under `member-reports`, covering query, report, delete, member-report event inventory, wrapper coverage, irreversible acknowledgement for deleting all reports for a member, and focused local tests.
- Added the current callable Wix Members Activity Members Followers family as explicit named commands under `members-followers`, covering follow, unfollow, follower/following lists, connection queries, follower event inventory, wrapper coverage, irreversible acknowledgement for unfollow, and focused local tests.
- Added the current callable Wix Members User Member family as an explicit read-only command under `user-members`, covering query, official inventory, wrapper coverage, and focused local tests.
- Added the current callable Wix Members About V2 family as explicit named commands under `member-abouts`, covering create, get, update, delete, query, get-my, member-about event inventory, wrapper coverage, current-revision validation for update, irreversible acknowledgement for delete, and focused local tests.
- Added the current callable Wix Members Privacy family as explicit named commands under `member-privacy`, covering default privacy reads/writes, member privacy settings reads/writes, Developer Preview inventory for Set Default Privacy Status, current-revision validation for settings updates, and focused local tests.
- Added the current callable Wix Members Custom Fields family as explicit named commands under `member-custom-fields`, covering create, update, delete, get, hide, list, and update-order with reviewed-plan write safety and irreversible acknowledgement for delete.
- Added the current callable Wix Members Custom Field Applications family as explicit named commands under `member-custom-field-applications`, covering create, update, delete, get, list-applications, get-members, and get-roles with full-application update notes and irreversible acknowledgement for delete.
- Added the current callable Wix Members Custom Field Suggestions family as explicit read commands under `member-custom-field-suggestions`, covering query and Developer Preview list.
- Added the current callable Wix Members Member Authentication family as an explicit reviewed-plan command under `member-authentication`, covering Developer Preview set-password email delivery with irreversible acknowledgement for the one-time email side effect.
- Added the current callable Wix Contact Extended Fields family as explicit named commands under `contact-extended-fields`, covering get, list, query, find-or-create, update, delete, callback-only event inventory, reviewed-plan writes, and irreversible acknowledgement for delete.
- Added the current callable Wix Contact Notes family as explicit named commands under `contact-notes`, covering get, query, create, update, delete, callback-only event inventory, reviewed-plan writes, revision notes for update, and irreversible acknowledgement for delete.
- Added the current callable Wix Contact Attachments family as explicit named commands under `contact-attachments`, covering get, list, generate upload URL, delete, callback-only event inventory, reviewed-plan writes, and irreversible acknowledgement for delete.
- Expanded the current callable Wix Contacts V4 family under `contacts`, covering facets, bulk-job read, merge preview, create, update, delete, merge, label/unlabel, bulk delete, bulk update, bulk label/unlabel, callback-only event inventory, reviewed-plan writes, revision checks, and stronger acknowledgement for destructive or broad jobs.
- Added the current callable Wix Form Schemas family as explicit named commands under `form-schemas`, covering active/deleted form reads, query/count helpers, provider configs, summaries, create/update/clone/bulk writes, trash restore/removal, callback-only event inventory, reviewed-plan writes, and irreversible acknowledgement for destructive or permanent removals.
- Added the current callable Wix Forms Chat Settings family as explicit named commands under `chat-settings`, covering get, query, create, update, delete, callback-only event inventory, revision validation for update, reviewed-plan writes, and irreversible acknowledgement for delete.
- Added the current Developer Preview Wix Forms Interactive Form Sessions family as explicit named commands under `interactive-form-sessions`, covering create, create streamed, send user message, send user message streamed, generate form summary, event-stream response handling, reviewed-plan writes, and focused local tests.
- Added the current Wix Forms Intake Forms and Intake Form Submissions families as explicit named commands under `intake-forms` and `intake-form-submissions`, covering query/search/count/list-data helpers, link generation, archive/unarchive/expiration writes, submission cancel/extend/exempt/delete writes, official path oddities, and stronger acknowledgement for form delete, submission cancel, and submission delete.
- Added the current callable Wix Community Groups core family as explicit named commands under `community-groups`, covering list, get, get by slug, query, create, update, delete, callback-only group event inventory, reviewed-plan writes, and irreversible acknowledgement for delete.
- Added the current callable Wix Community Group Rules family as explicit named commands under `community-group-rules`, covering list, create-or-replace, callback-only rules-updated inventory, replacement-write safety, and irreversible acknowledgement for replacing all existing rules.
- Added the current callable Wix Community Create Group Requests family as explicit named commands under `community-group-requests`, covering list, query, approve, reject, callback-only approved/rejected event inventory, and irreversible acknowledgement for approval/rejection decision writes.
- Added the current callable Wix Community Group Members family as explicit named commands under `community-group-members`, covering group-member reads, membership reads, member add/remove writes, callback-only member event inventory, and irreversible acknowledgement for membership changes.
- Added the current callable Wix Community Group Roles family as explicit named commands under `community-group-roles`, covering assign/unassign writes, callback-only role event inventory, and irreversible acknowledgement for permission-changing role writes.
- Added the current callable Wix Community Join Group Requests family as explicit named commands under `community-join-requests`, covering list, query, approve, reject, callback-only join-request event inventory, and irreversible acknowledgement for private-group membership decisions.
- Added the current callable Wix Community Membership Questions family as explicit named commands under `community-membership-questions`, covering question reads, answer reads, full-set create-or-replace writes, and irreversible acknowledgement for replacing all membership questions.
- Added the current callable Wix Community Comments family as explicit named commands under `community-comments`, covering comment reads, count/list/thread helpers, create/update writes, single-comment moderation writes, bulk moderation writes, callback-only comment event inventory, and irreversible acknowledgement for deletes, moderation-state writes, and all bulk comment actions.
- Added the current callable Wix Community Reports V2 family as explicit named commands under `community-reports`, covering report reads, query/count helpers, create/update/upsert writes, callback-only report event inventory, and irreversible acknowledgement for report delete and bulk delete by filter.
- Added the current callable Wix Community Reviews family as explicit named commands under `community-reviews`, covering get/query/count reads, create/update/delete, bulk create/delete, reply writes/removal, moderation-status writes, callback-only review event inventory, stores-only notes, and irreversible acknowledgement for delete, reply removal, moderation changes, and bulk actions.
- Added the current callable Wix Community Review Requests family as explicit named commands under `community-review-requests`, covering create/get/delete/query/count, bulk cancel by filter, callback-only review-request event inventory, stores-only notes, canceled-only delete notes, and irreversible acknowledgement for delete and bulk cancel.
- Added the current callable Wix Community Moderation Rules family as explicit named commands under `community-moderation-rules`, covering rule reads, query, check-content, create/update/delete writes, callback-only rule event inventory, and irreversible acknowledgement for moderation policy changes.
- Added the current callable Wix Inbox Conversations and Inbox Messages families as explicit named commands under `inbox-conversations` and `inbox-messages`, covering conversation get/get-or-create, message list/send, callback-only Inbox event inventory, stronger approval for message sends, and a docs-only Channels note because the current official Inbox menu has no separate callable Channels method page.
- Added the current callable Wix Loyalty Program Management Program slice as explicit named commands under `loyalty-program`, covering program reads, premium feature reads, program update, activate, pause, points-expiration enable/disable, callback-only event inventory, and irreversible acknowledgement for program-wide writes.
- Added the current callable Wix Loyalty Program Management Earning Rules slice as explicit named commands under `loyalty-earning-rules`, covering rule reads, create/update/delete, bulk create, custom automated-rule create, custom automation delete, callback-only event inventory, and irreversible acknowledgement for earning-rule writes.
- Added the current callable Wix Loyalty Program Management Tiers slice as explicit named commands under `loyalty-tiers`, covering tier reads, tier lifecycle writes, bulk create, get program, global tier program settings, callback-only event inventory, and irreversible acknowledgement for tier writes.
- Added the current callable Wix Loyalty Accounts slice as explicit named commands under `loyalty-accounts`, covering deprecated list compatibility, account get/query/search/count, program totals, current-member and secondary-ID lookup, create account, adjust points, bulk adjust points, earn points, callback-only account event inventory, and irreversible acknowledgement for account and point-balance writes.
- Added the current callable Wix Loyalty Rewards definitions subfamily as explicit named commands under `loyalty-rewards`, covering reward list/get/query/create/bulk-create/update/delete, callback-only reward event inventory, and irreversible acknowledgement for reward-definition writes.
- Added Wix Loyalty Checkout Discount as explicit named commands under `loyalty-checkout-discounts`, covering discount query/apply and irreversible acknowledgement for applying checkout rewards that can redeem points or use customer loyalty/referral rewards.
- Added Wix Loyalty Coupons as explicit named commands under `loyalty-coupons`, covering coupon get/query/current-member reads, point redemption and delete writes, callback-only coupon events, and irreversible acknowledgement for redemption/delete flows.
- Added Wix Loyalty Transactions as explicit read-only named commands under `loyalty-transactions`, covering transaction get/query and splitting the broad remaining Loyalty Program CRM row into exact account subfamilies.
- Added Wix Loyalty Social Media as explicit named commands under `loyalty-social-media`, covering followed-channel list/create, visitor/member auth notes, callback-only followed-channel event inventory, and irreversible acknowledgement for create because it can award loyalty points.
- Added Wix Loyalty Imports as explicit named commands under `loyalty-imports`, covering import get/query, upload URL creation, import creation/execution, error-file download URL reads, callback-only import event inventory, and irreversible acknowledgement for create/execute because imports can overwrite customer point balances.
- Added Wix CRM Tasks as explicit named commands under `crm-tasks`, covering task create/get/update/delete/query/count/move-after, callback-only task events, revision validation for update, and irreversible acknowledgement for delete.
- Added Wix CRM Pipelines as explicit Developer Preview named commands under `crm-pipelines`, covering pipeline create/get/update/delete/query, bulk tag updates, callback-only pipeline events, revision validation for update, and irreversible acknowledgement for delete and filtered bulk tag updates.
- Added Wix CRM Cards as explicit Developer Preview named commands under `crm-cards`, covering card create/get/update/delete/query/search, bulk tag updates, move, search by stage, callback-only card events, revision validation for update, and irreversible acknowledgement for delete and filtered bulk tag updates.
- Added Wix AI Site-Chat as explicit named commands split across `ai-site-chat-widget-settings`, `ai-site-chat-widget-settings-v2`, `ai-site-chat-conversations`, and `ai-site-chat-messages`, covering deprecated v1 settings, Developer Preview v2 settings, visitor-scoped conversation/message reads, message bulk creation, and stronger acknowledgement for sending chat messages.
- Added Wix Analytics Sessions as explicit beta named commands under `analytics-sessions`, covering list job result reads, list async job creation, recording deletion markers, session recorded markers, required list filter validation, and stronger acknowledgement for recording-state mutations.
- Added the current callable Wix Communication Email Subscriptions family as explicit named commands under `email-subscriptions`, covering query, upsert, bulk upsert, unsubscribe-link generation, Developer Preview notes, callback-only event inventory, and reviewed-plan writes.
- Added the current callable Wix Portfolio Project Items family as explicit named commands under `portfolio-project-items`, covering create, get, update, delete, list, bulk create, bulk update, bulk delete, duplicate, project-item event inventory, wrapper coverage, before-state capture for updates/deletes, irreversible acknowledgement for delete and bulk delete, and focused local tests.
- Added the current callable Wix Portfolio Projects family as explicit named commands under `portfolio-projects`, covering create, get, update, delete, query, list, bulk update, project event inventory, wrapper coverage, current-revision handling for update and bulk update, irreversible acknowledgement for delete, the current official bulk-update path oddity, and focused local tests.
- Added a disabled/non-callable Wix Forum inventory boundary covering historical category/post methods and events, with docs explaining that Wix discontinued Forum on March 1, 2026 and forum data was deleted.
- Added the current callable Wix Portfolio Collections family as explicit named commands under `portfolio-collections`, covering create, get, update, delete, query, list, collection event inventory, wrapper coverage, current-revision handling for update, irreversible acknowledgement for delete, the current official permission-label mismatch, and focused local tests.
- Added the current callable Wix Blog Likes family as explicit named commands under `blog-likes`, covering create, get, delete, query, delete by FQDN and entity ID, like event inventory, wrapper coverage, irreversible acknowledgement for deletes, and focused local tests.
- Added the current callable Wix Blog Tags family as explicit named commands under `blog-tags`, covering get, delete, query, create, get by label, get by slug, tag event inventory, wrapper coverage, irreversible acknowledgement for delete, and focused local tests.
- Added the current callable Wix Blog Categories family as explicit named commands under `blog-categories`, covering create, get, update, delete, query, list, get by slug, category event inventory, wrapper coverage, irreversible acknowledgement for delete, and focused local tests.
- Added the current callable Wix Blog Draft Posts family as explicit named commands under `blog-draft-posts`, covering create, get, update, delete, query, list, bulk create/update/delete, deleted draft reads, publish, remove from trash, restore from trash, official inventory, wrapper coverage, irreversible acknowledgement for permanent or broad deletes, and focused local tests.
- Added the current callable Wix Blog Posts & Stats family as explicit read-only commands under `blog-posts-stats`, covering get, query, list, get by slug, post metrics, total posts, monthly post counts, official inventory, wrapper coverage, and focused local tests.
- Added the current callable Wix Restaurants Experiences family as explicit named commands under `restaurants-reservation-experiences`, covering create, get, update, query, search, bulk tag updates, get by slug, Developer Preview documentation, revision checks for update, irreversible acknowledgement for broad filter tag updates, official inventory, wrapper coverage, and focused local tests.
- Added the current callable Wix Restaurants Reservation Time Slots family as explicit read-only commands under `restaurants-reservation-time-slots`, covering check, get scheduled time slots, get nearby time slots, Developer Preview documentation, official inventory, wrapper coverage, and focused local tests.
- Added the current callable Wix Restaurants Reservation Locations family as explicit named commands under `restaurants-reservation-locations`, covering get/list/query reads, plan-first update, Developer Preview documentation, revision checks for update, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Reservations family as explicit named commands under `restaurants-reservations`, covering get/list/query/search helpers, create/update/delete, bulk archive/unarchive, cancel, create-held, reserve, Developer Preview documentation, revision checks for update, irreversible acknowledgement for delete and cancel, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Online Orders Notification Recipients family as explicit named commands under `restaurants-online-order-notification-recipients`, covering get/query helpers, create/update/delete, bulk create/update/delete, tag updates, Developer Preview documentation, revision checks for update flows, irreversible acknowledgement for delete, bulk delete, and broad filter tag updates, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Online Orders Service Fees family as explicit named commands under `restaurants-online-order-service-fees`, covering calculate/list/get/query helpers, create/update/delete, bulk create/update/delete, tag updates, Developer Preview documentation, revision checks for update flows, irreversible acknowledgement for delete, bulk delete, and broad filter tag updates, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Online Orders Availability Exceptions family as explicit named commands under `restaurants-online-order-availability-exceptions`, covering get/query, create/update/delete, bulk create/update, tag updates, Developer Preview documentation, revision checks for update flows, irreversible acknowledgement for delete and broad filter tag updates, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Online Orders Fulfillment Methods family as explicit named commands under `restaurants-online-order-fulfillment-methods`, covering list/get/query, address and availability helpers, create/update/delete, bulk create, tag updates, Developer Preview documentation, deprecated availability helpers, revision checks for update, irreversible acknowledgement for delete and broad filter tag updates, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Online Orders Menu Ordering Settings family as explicit named commands under `restaurants-online-order-menu-ordering-settings`, covering get/query/list availability helpers, update, bulk update, tag updates, update extended fields, upsert by menu ID, Developer Preview documentation, revision checks for update flows, irreversible acknowledgement for broad filter tag updates, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Online Orders Operations family as explicit named commands under `restaurants-online-order-operations`, covering operation get/list/query helpers, availability and address helpers, update/delete, bulk tag updates, Developer Preview documentation, revision checks for update, irreversible acknowledgement for delete and broad filter tag updates, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Online Orders Operation Groups family as explicit named commands under `restaurants-online-order-operation-groups`, covering operation-group get/query helpers, create/update/delete, bulk create/delete/update, bulk tag updates, revision checks for update flows, irreversible acknowledgement for delete, bulk delete, and broad filter tag updates, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Item Modifier Groups family as explicit named commands under `restaurants-item-modifier-groups`, covering modifier-group list/get/query/count helpers, create/update/delete, bulk create/update, Developer Preview documentation, revision checks for update flows, irreversible acknowledgement for delete, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Item Modifiers family as explicit named commands under `restaurants-item-modifiers`, covering modifier list/get/query/count helpers, create/update/delete, bulk create/delete/update, Developer Preview documentation, revision checks for update flows, irreversible acknowledgement for delete and bulk delete, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Item Variants family as explicit named commands under `restaurants-item-variants`, covering variant list/get/query/count helpers, create/update/delete, bulk create/delete/update, Developer Preview documentation, revision checks for update flows, irreversible acknowledgement for delete and bulk delete, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Item Labels family as explicit named commands under `restaurants-item-labels`, covering label list/get/query helpers plus create/update/delete, Developer Preview documentation, revision checks for update, irreversible acknowledgement for delete, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Restaurants Sections family as explicit named commands under `restaurants-sections`, covering section list/get/query helpers, create/update/delete, bulk create/delete/update, duplicate, Developer Preview documentation, revision checks for update flows, irreversible acknowledgement for delete and bulk delete, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Events Form family as explicit named commands under `events-forms`, covering registration form reads, draft/control/message plans, deprecated draft discard/publish handling, irreversible acknowledgement for draft discard, control deletion, message updates, and draft publish, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Events Orders and checkout family as explicit named commands under `events-orders`, covering order reads/list, order summary, checkout options, available-ticket reads, invoice previews, order update plans, bulk updates, checkout updates, irreversible acknowledgement for confirm/checkout/deprecated reservation hold and release actions, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Events Tickets family as explicit named commands under `events-tickets`, covering ticket reads/list, ticket update plans, bulk archived-state updates, check-in, irreversible acknowledgement for deleting check-in evidence, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Events Ticket Reservations family as explicit named commands under `events-ticket-reservations`, covering reservation reads, create plans, known-ID bulk tag updates, irreversible acknowledgement for delete, by-filter bulk tag updates, and cancellation, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Events RSVP V2 family as explicit named commands under `events-rsvps-v2`, covering RSVP reads, query/search/count, summaries, create/update/bulk-update plans, guest check-in, irreversible acknowledgement for delete, bulk delete, and check-in cancellation, docs, official inventory, wrapper coverage, and focused local tests.
- Added the current Wix Events Event Guests read family as an explicit named command under `events-guests`, covering guest query reads, `guestDetails` fieldset notes, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Events Staff Members family as explicit named commands under `events-staff-members`, covering staff member reads, create/update plans, permanent delete acknowledgement, revision notes for update, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Events Policies V2 family as explicit named commands under `events-policies-v2`, covering policy reads, create/update/reorder plans, permanent delete acknowledgement, revision notes for update, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Events Schedule Items family as explicit named commands under `events-schedule-items`, covering schedule item reads, draft schedule writes, member bookmarks, publish/reschedule helpers, irreversible acknowledgement for delete and discard-draft, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Bookings Writer V2 family as explicit named commands under `bookings-writer-v2`, covering single-service booking writes, multi-service booking reads/writes, allowed-action helpers, anonymous action-token flows, reviewed-plan safety, irreversible acknowledgement for higher-risk lifecycle changes, the official 12-booking bulk-create cap, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Booking Policies family as explicit named commands under `bookings-policies`, covering policy reads, query/count helpers, strictest-policy calculation, reviewed-plan policy writes, irreversible acknowledgement for delete and default-policy changes, revision checks for update, docs, official inventory, wrapper coverage, and focused local tests.
- Added `bookings-policy-snapshots list` for the official read-only Booking Policy Snapshots method, keeping the Booking Policy Service Plugin as a Developer Preview service-plugin boundary instead of a normal CLI command.
- Added the full current Wix Bookings Attendance family as explicit named commands under `bookings-attendance`, covering get/query/count helpers, reviewed-plan set and bulk-set writes, irreversible acknowledgement for delete and bulk-delete, Developer Preview/member-auth-only handling for count, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Bookings Waitlist family as explicit named commands under `bookings-waitlist`, covering list/register/leave/book, Developer Preview documentation, event-session acknowledgement for writes, irreversible acknowledgement for leave and book, docs, official inventory, wrapper coverage, and focused local tests.
- Accounted Wix Bookings Validation Service Plugin as a callback-only Developer Preview service-plugin boundary in coverage, official inventory, wrapper docs, and proof without exposing CLI commands.
- Accounted Wix Booking Policy Service Plugin as a callback-only Developer Preview service-plugin boundary in coverage, official inventory, wrapper docs, and proof without exposing CLI commands.
- Expanded Bookings Time Slots V2 from appointment availability only to the current read-only availability surface with `list-event`, `get-event`, `list-multi-service`, and `get-multi-service`, keeping `get-event` marked Developer Preview and course/booking-write flows outside these commands.
- Added the full current Wix Bookings Services V2 family as explicit named commands under `bookings-services-v2`, with read/helper commands, reviewed-plan writes, irreversible acknowledgement for destructive or replace-style service operations, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Bookings Resources V2 family as explicit named commands under `bookings-resources-v2`, with read/helper commands, reviewed-plan writes, irreversible acknowledgement for delete operations, revision checks for update, 50-item bulk limits, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Bookings Resource Types V2 family as explicit named commands under `bookings-resource-types-v2`, with read/helper commands, reviewed-plan writes, irreversible acknowledgement for delete because Wix deletes connected resources, revision checks for update, docs, official inventory, wrapper coverage, and focused local tests.
- Added the full current Wix Bookings Staff Members family as explicit named commands under `bookings-staff-members`, with active/deleted staff reads, reviewed-plan writes, irreversible acknowledgement for staff deletion and trash-bin removal, revision checks for update, 100-staff bulk tag limits, docs, official inventory, wrapper coverage, and focused local tests.
- Added `payments transactions-list` for the official Cashier Payments transaction-list read on `GET /payments/v2/transactions`, including explicit query filter flags, docs, inventory, wrapper coverage, and focused local tests.
- Expanded Wix Stores Brands V3 from read/query only to the full current method set: create, update, delete, bulk create, bulk delete, bulk update, get-or-create, and bulk get-or-create, with plan-first write gates and irreversible acknowledgement for deletes.
- Expanded Wix Stores Ribbons V3 from read/query only to the full current method set: create, update, delete, bulk create, bulk delete, bulk update, get-or-create, and bulk get-or-create, with plan-first write gates and irreversible acknowledgement for deletes.
- Expanded Wix Stores Info Sections V3 from core CRUD to the full current method set: bulk create, bulk delete, bulk update, get-or-create, and bulk get-or-create, with plan-first write gates, revision checks for bulk update, and irreversible acknowledgement for bulk delete.
- Expanded Wix Stores Customizations V3 from read/query only to the full current method set: create, update, delete, bulk create, bulk update, add choices, bulk add choices, remove choices, and set choices, with plan-first write gates, revision checks for update methods, and irreversible acknowledgement for delete/remove/set choice operations.
- Expanded Wix Stores Categories from the first read slice to the full current method set: get by slug, create, update, delete, bulk update, visibility changes, bulk show, item/category assignment changes, move, and set arranged items, with plan-first write gates, revision checks for update methods, and irreversible acknowledgement for delete/remove/set-arranged operations.
- Expanded Wix Stores Products V3 from core reads plus create/update to the full current method set: delete, bulk create/delete/update, create/update with inventory, bulk create/update with inventory, info-section helpers, category helpers, and variant filter helpers, with plan-first write gates, revision checks for update methods, inventory permission notes, and irreversible acknowledgement for destructive delete/remove operations.
- Added the first Restaurants slice (`restaurants-menus`) as explicit named commands: `list`, `get`, `query`, `create`, `update`, `delete`, `bulk-create`, `bulk-update`, `duplicate`, and `update-extended-fields` under the `/restaurants/menus-menu/v1` official endpoints, with reviewed-plan write gates, `--ack-irreversible` on destructive actions, revision requirements for update flows, and docs/inventory/wrapper/proof/test parity.

### Changed
- Added the official Automations Storage Items and Automations V2 families to the shipped command surface with explicit `automation-storage-items` and `automations-v2` commands, reviewed-plan writes, irreversible acknowledgement for filtered storage tag updates and workflow create/update/delete, callback-only event tracking, docs, inventory, wrapper coverage, and focused local tests.
- `docs/api_coverage.md` remains the full-goal Wix ledger. As of the final 2026-06-30 source-ready pass, every real callable row is implemented and all non-command surfaces are accounted with an explicit limited status.
- Expanded Order Billing from the earlier refund subset to the full current official family with `authorize-charge-with-saved-payment-method`, `capture-authorized-payments`, `void-authorized-payments`, `generate-receipts`, `redeem-gift-card`, and the existing refundability/refund commands; kept explicit reviewed-plan safety, `--ack-irreversible` on capture/void/redeem/refund, Developer Preview notes on receipts and gift-card redemption, and aligned the focused proof to a `79`-test, `0`-failure rerun on `2026-06-24`.
- Added the current eCommerce Orders subset to the shipped surface with `orders search|get|create|update|cancel|bulk-update`, kept it explicit that this is only the Orders slice and not full eCommerce, and aligned the family ledger, wrappers, public command docs, official inventory, and focused proof with a `75`-test, `0`-failure rerun on `2026-06-24`.
- Added the official Email Marketing Campaign lifecycle writes to the shipped surface with `email-campaigns publish|reuse|delete`, reviewed-plan safety for all three, `--ack-irreversible` for delete, publish landing-page-only behavior when no email distribution body is supplied, reuse copy verification, and the refusal-before-HTTP tests for direct `--apply --yes` without `--plan-in`.
- Aligned the Email Marketing Campaign family ledger, wrappers, public command docs, and focused proof with the shipped lifecycle-write surface; the current focused rerun is `88` tests, `0` failures on `2026-06-24`.
- Added the official Referral Program Program slice with `referral-program get|get-premium-features|get-ai-social-media-posts-suggestions|activate|pause|generate-ai-social-media-posts-suggestions|update`, reviewed-plan safety for writes, revision enforcement for update, Program Updated as callback-only, and exact open rows for Referral Rewards, Referring Customers, Referred Friends, and Referral Tracker.
- Added the official Referral Tracker slice with read-only `referral-tracker get|query|get-statistics`, exact official paths, required query-object validation, and Referral Event Created as callback-only.
- Classified official Account Level Sites Skills as docs-only because the Wix pages are recipes around existing APIs, not a standalone callable REST family; no `sites-skills` command is exposed.
- Added the official Account Level Resellers package/product-instance slice with `resellers get|query|create-package|adjust-product-instance|assign-product-instance|unassign-product-instance|update-package-external-id|cancel-package|cancel-product-instance`, account-level API-key auth, reviewed-plan writes, and irreversible acknowledgement for cancel commands.
- Split the broad Multilingual row into exact official subfamilies and added the Locale Settings slice with `multilingual-locale-settings get|set-mode|update`, reviewed-plan writes, revision validation for update, and irreversible acknowledgement when disabling multilingual mode.
- Added the official Multilingual Locales slice with `multilingual-locales create|get|update|delete|query|bulk-create|bulk-delete|bulk-update|create-new-primary|get-new-primary-status|list-supported|set-visitor-primary`, reviewed-plan writes, and irreversible acknowledgement for locale deletes and primary-locale changes.
- Added the official Referral Rewards read slice with `referral-rewards get|query`, official query filter/sort notes, inventory coverage, command docs, wrapper docs, and focused tests.
- Added the official Referring Customers slice with `referring-customers get|query|get-by-referral-code|generate-for-contact|delete`, official filter/sort notes, plan-first safety for writes, `--ack-irreversible` for delete, inventory coverage, command docs, wrapper docs, and focused tests.
- Added the official Referred Friends slice with `referred-friends get|query|get-by-contact-id|create|update|delete`, official filter/sort notes, plan-first safety for writes, `--ack-irreversible` for delete, inventory coverage, command docs, wrapper docs, and focused tests.
- Added the official Benefit Programs Balances subset to the shipped surface with `balances get|list|query|change|revert-change`, Pricing Plans app preflight, reviewed-plan safety for live credit changes, and a focused proof result of `55` tests, `0` failures on `2026-06-24`.
- Added the official Benefit Programs Items subset to the shipped surface with `benefit-items get|list|query|count|create|update|delete|bulk-create|bulk-delete|bulk-update|bulk-delete-by-filter`, Pricing Plans app preflight, reviewed-plan safety for writes, `--ack-irreversible` on destructive writes, and a focused proof result of `60` tests, `0` failures on `2026-06-24`.
- Aligned the public coupons contract wording for the already-shipped `coupons get|query|create|update|delete|bulk-create|bulk-delete` family, including the installed-app gate, `Manage Coupons` permission, live-read vs reviewed-plan write split, `--ack-irreversible` for delete/bulk-delete, the 100-item query cap, case/space-sensitive codes, and the one-coupon-per-order rule. The tool remains partial, and the latest recorded full-suite baseline is now `746` tests, `0` failures on `2026-06-24`.
- Added the official Donation Campaigns family to the shipped subset, with `donation-campaigns get|get-metrics|query|create|update|bulk-create|bulk-update|bulk-update-tags|bulk-update-tags-by-filter`, reviewed-plan safety for writes, the empty-filter retagging boundary kept explicit, and the new focused proof result of `56` tests, `0` failures on `2026-06-24`.
- Added `pricing-plans get`, `pricing-plans query`, `pricing-plans search`, `pricing-plans count`, `pricing-plans create`, `pricing-plans update`, `pricing-plans delete`, and `pricing-plans bulk-update` for the official Pricing Plans V3 family, with read/write permission notes, reviewed-plan safety for writes, `--ack-irreversible` for delete, explicit docs about query defaults, search aggregations, the current `{plan.id}` update-page placeholder quirk, the bulk-update `100`-plan cap, and the official no-plan-renames rule.
- Added `branches get-default`, `branches get`, and `branches query` for the official read-only branch metadata API, with app/user auth notes, `Manage Site Branches`, and the editor-only content-editing limit kept explicit.
- Added `auth token refresh` for the official legacy Wix refresh-token endpoint, with local token-file reuse, redacted output, and explicit docs that the flow is deprecated for new apps.
- Added `site-plugins get-placement-status` for the official Site Plugins placement-status read, with explicit site-context auth and permission notes.
- Added `market-listing search` for the official Wix App Market listing search method, with explicit Developer Preview notes, published-listings-only behavior, and a bounded `--limit` guard up to 50.
- Added `embedded-scripts get` for the official Embedded Scripts read method, with optional `--component-id` and explicit docs about the current auth wording mismatch between the family intro and the method page.
- Added `embedded-scripts embed` for the official Embedded Scripts write method, with reviewed-plan apply gates, before-state snapshots from the read method, and read-after-write verification.
- Added `auth token request` for the official legacy custom-auth authorization-code exchange on `POST /oauth/access`, with safe local token-file storage and explicit deprecated-for-new-apps docs.
- Added `bi-event send` for the official BI Event method, with explicit `--event-name`, optional JSON `--event-data-json`, and reviewed-plan apply gating because sent events cannot be rolled back.
- Added `editor-deep-link create` for the official Editor Deep Link helper method, with optional `customParams` string pairs and explicit legacy-custom-element-only notes.
- Added the broader Media Manager files family to the shipped command surface, with explicit named commands for `update`, `bulk-delete`, `bulk-restore`, `generate-upload-url`, `generate-resumable-upload-url`, `import`, and `generate-download-url`.
- Added the official Media Manager folders family to the shipped command surface, with explicit named commands and reviewed-plan writes for `create`, `update`, `bulk-delete`, and `bulk-restore`.
- Added the official App Installation family to the shipped command surface, with `app-installation get-installed` and `app-installation is-permitted` kept as read/preflight commands, the five install/uninstall methods shipped as reviewed-plan writes, returned `appToken` values redacted, and the app/user-token vs logged-in-user/API-key-admin wording kept explicit.
- Added the official CMS collection folders family to the shipped command surface, with explicit named commands for folder reads, folder writes, and collection-reference reads/writes.
- Added the real `analytics-data get` command behind the existing parser surface, with explicit measurement-type validation and the documented recent-data window.
- Added the official Wix Analytics Semantic Models family to the shipped command surface with `analytics-semantic-models list`, `analytics-semantic-models get`, and `analytics-semantic-models query`, plus explicit `semanticModelId` and required `interval` validation.
- Added the official Wix Async Job family to the shipped command surface with `async-jobs get` and `async-jobs list-items`, keeping explicit named read methods without reintroducing the removed generic jobs runner.
- Added the official Wix Site Search family to the shipped command surface with `site-search search`, explicit document-type validation, explicit `search` JSON input, and honest docs about the current `/_api/site-search/v1/search` vs `/v1/search` docs mismatch.
- Added Business Management `tags` to the shipped subset and to the docs/proof alignment, with risky write commands now enforcing reviewed-plan apply gates in the real CLI runtime.
- Repaired proof/docs alignment from the latest local pass; the newest focused App Management proof is `76` tests with `0` failures.
- Kept the parser state explicitly short of release claims for the full Wix goal.

## [0.1.0] - 2026-06-22

### Earlier progress snapshot
- Frozen the shipped Wix boundary in `docs/api_coverage.md` with explicit `implemented`, `gated`, `callback-only`, and `excluded` labels.
- Aligned README, auth docs, onboarding, proof, wrapper docs, and the public `wix` skill wrapper to the same boundary wording and safety contract for the current Wix subset.
- Recorded the earlier partial-subset finish snapshot for that milestone. The current proof for the repo state has moved forward since then and is tracked in `docs/proof.md`.

### Added
- Added the first bounded Contributors write command: `contributors remove`, with `--account-id` + `--site-id`, app-token/site-user auth, dry-run planning, `--apply --yes --ack-irreversible` gating, and read-after-write contributor verification.
- Added `contributors change-role`, with `--account-id` + `--site-id`, explicit role GUID input via `--role-ids-json`, dry-run planning, `--apply --yes` gating, and provider-response-plus-readback verification for full role replacement.
- Added `contributors change-contributor-location`, with `--account-id` + `--site-id`, explicit location GUID input via `--location-ids-json`, dry-run planning, `--apply --yes` gating, and provider-response-plus-readback verification for full location replacement.
- Added account-level AI Credits balance read: `ai-credits get-balance`, with API-key-only auth in this boundary, no `wix-account-id` header, and read-only request flow.
- Added account-level Domain DNS reads: `domain-dns get-zone` and `domain-dns preview-zone`, with explicit API-key auth notes, hostname validation, and safe preview/get-only scope.
- Added Domain DNS writes and DNS propagation read coverage: `domain-dns create-zone`, `domain-dns update-zone`, `domain-dns delete-zone`, and `dns-propagation get`, with reviewed-plan write gates, `--ack-irreversible` where destructive replacement or deletion applies, and official account-level DNS notes from Wix docs.
- Added account-level Connected Domains reads: `connected-domains list`, `connected-domains get`, and `connected-domains get-setup-info`, with explicit account-header auth notes and conservative paging/domain validation.
- Added account-level Domain Search reads: `domains check-availability` and `domains suggest`, with conservative query validation and explicit API-key auth notes from official Wix docs.
- Added account-level `projects create-project` with local `WIX`-only `--type` guardrail, required `--name`, support for optional `--template-id`, `--folder-id`, and `--apps-json`, and response-only success proof using returned `project.metaSiteId` and `project.siteId`.
- Added account-level `site-actions duplicate` with source-site preflight, plan-first safety, and provider-response/new-site verification.
- Added account-level `site-actions publish` with plan-first safety, API-key `wix-site-id` auth, `POST /site-publisher/v1/site/publish` call behavior (empty-object success response), and read-back verification on `published=true`.
- Added account-level `site-folders` commands for query, get-folder-by-site, create, update, delete, and move-sites.
- Added account-level `site-actions bulk-delete` with preflight site reads, plan-first safety, and provider-response verification.
- Replaced the template front-door identity with a Wix-specific README contract for `Wix`.
- Added source-start `docs/api_coverage.md` with category-level boundary mapping and status labels.
- Added `docs/references.md` with official Wix source URLs used for this milestone.

### Changed
- Updated proof and audit framing for the Wix safe CLI boundary to the later 2026-06-22 re-audit runs; current exact counts live in `docs/proof.md`.
- Updated skill-wrapper and command-surface contract docs to include the shipped `wix` wrapper and keep command lists aligned.
- Updated `.env.example` from template placeholders to Wix-first defaults.
- Updated `src/wix_safe_agent_cli/commands/onboarding.py` base URL hint text to Wix defaults.
- Updated docs-facing tests for the new public readme contract.

### Removed
- Removed `TEMPLATE_GUIDE.md` from the tool package.

### Fixed
- CLI template: ensure argument/usage errors in `--output json` mode emit exactly one JSON error object (no argparse usage text).
