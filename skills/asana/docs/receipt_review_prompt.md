# Review an Asana receipt

Use this after an approved apply. Do not treat provider acceptance or a saved before-state as proof of completion or rollback.

```text
Review this Asana receipt against the approved plan and my original request.

Tell me whether Asana accepted the request, the current asynchronous state, what the verification read proved, and what is still unverified. Check that the receipt uses the approved plan ID and expected command. If the provider failed or verification did not match, explain the smallest safe next read or new plan. Do not run a restore, retry a write, or promise rollback automatically.

Original request:
<describe the intended Asana change>

Approved plan:
<paste or load the saved plan>

Receipt:
<paste or load the saved receipt>
```
